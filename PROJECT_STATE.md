# AviorCMS Android — состояние проекта (читать в начале каждой новой сессии)

> Аналог `PROJECT_STATE.md` из репозитория `cms_avior` (веб-CRM), только
> для мобильного приложения. Положен в репозиторий 19.08.2026, сразу
> после того как Вейс залил свою рабочую версию приложения (совпала по
> структуре файлов с тем, что было собрано в чате в этот же день —
> расхождений не нашлось, фикс счётчиков из §4 применён тут же).

## 1. Что это

Внутреннее Android-приложение для сотрудников сервиса АВИОР (владелец,
админы, инженеры-приёмщики — та же ролевая модель, что в веб-CRM).
Работает через **Mobile API** веб-CRM: `https://cms.avior.moscow/api/mobile/`.
Полная спецификация API — в репозитории `cms_avior`, файл
`docs/MOBILE_API.md` (там же — актуальный «промт для Gemini» с описанием
дизайна/экранов, если понадобится перегенерировать что-то через Gemini).

## 2. Репозиторий

- GitHub: `https://github.com/vahmedov-code/avior_crm_apk.git`
- Токен для пуша — fine-grained, выдаётся Вейсом заново в каждой сессии
  (не хранится между сессиями, спрашивать при необходимости запушить).
- **19.08.2026** — Вейс запушил свою рабочую версию (проверено: структура
  файлов совпадает с тем, что было собрано в этой же сессии — см. §3,
  расхождений не найдено, сводить две версии не пришлось).

## 3. Что собрано (в репозитории, версия Вейса — идентична по структуре)

Полноценный проект с нуля — из-за проблем со сборкой у Gemini
(см. §5 «Известные грабли») собирали заново, аккуратно:

- **API-слой**: `ApiModels.kt` (data-классы строго под реальные названия
  полей из `docs/MOBILE_API.md` — `report_url`, `receipt_url`,
  `client_name` и т.д., через `@SerializedName`), `ApiService.kt`
  (Retrofit-интерфейс всех эндпоинтов), `ApiClient.kt` (единственный
  экземпляр Retrofit на всё приложение + `AuthInterceptor`, подставляющий
  токен в каждый запрос — важно: НЕ создавать второй Retrofit где-то ещё,
  иначе запросы оттуда уйдут без токена и будет выглядеть как «просит
  войти заново», хотя токен есть).
- **`TokenStore`** — `EncryptedSharedPreferences`, синхронное чтение
  (без гонки при старте приложения).
- **`CmsViewModel`** — единая точка состояния (`StateFlow`): заказы,
  клиенты, справочники (`meta`), логин/логаут, `refreshData()`.
- **Экраны** (`ui/`): `LoginScreen`, `DashboardScreen` (счётчики по
  статусам — аналог `.pipeline` на веб-панели), `OrderListScreen`,
  `AddOrderScreen`, `OrderDetailScreen` (документы/статус/шаринг),
  `ClientsScreen`, `Common.kt` (иконки устройств/цвета статусов/EmptyState).
- **Тема** (`ui/theme/`) — фирменные цвета АВИОР (`#1C2436` navy,
  `#E0B45C` gold), Light/Dark.
- **`build.gradle.kts`** — явные версии зависимостей (не через `libs.`
  каталог — раз он мог быть криво настроен раньше, лучше явно).

## 4. Известный баг и готовый фикс (проверить, применён ли)

**Симптом**: счётчики на главном экране «живут своей жизнью» — то 0,
то частичные данные.

**Причина**: `DashboardScreen` при заходе вызывал `viewModel.refreshData()`,
которая по инерции подгружала последний использованный фильтр статуса
(если до этого заходили в список заказов с фильтром) вместо полного
списка.

**Фикс** (проговорено с Вейсом, готов к применению): в `DashboardScreen.kt`
заменить
```kotlin
LaunchedEffect(Unit) { viewModel.refreshData() }
```
на
```kotlin
LaunchedEffect(Unit) {
    viewModel.loadMeta()
    viewModel.loadOrders(null)
    viewModel.loadClients(null)
}
```
**Статус: фикс применён 19.08** (был не применён в первой залитой версии
— заменено на месте, запушено этим же коммитом). Если увидите старый
код `viewModel.refreshData()` в `DashboardScreen.kt` где-то ещё — значит,
это регресс, вернуть на вариант ниже.

## 5. Известные грабли сборки (не наступать снова)

Всё это уже один раз ломало сборку у Gemini, разбирали долго — если
увидите похожее в коде Вейса или будете что-то генерировать заново:

- `compileSdk { version = release(37) }` — **невалидный/выдуманный
  синтаксис**. Правильно: `compileSdk = 34` (простое число).
- `buildTypes { release { optimization { enable = false } } }` —
  тоже невалидно. Правильно: `isMinifyEnabled = false`.
- `androidx.browser:browser` как `project("androidx.browser:browser")`
  — ломает Gradle (пытается найти как локальный путь). Если нужен
  Custom Tabs — `implementation("androidx.browser:browser:1.8.0")`.
  В итоге в приложении Custom Tabs не используются вообще (документы
  открываются через `Intent.ACTION_SEND`/`ACTION_SENDTO`), так что
  этой зависимости в проекте быть не должно.
- `Icons.AutoMirrored.Filled.ArrowBack` (и `.Send`, `.Assignment`) —
  **не резолвится** в текущей версии `material-icons-extended` в
  проекте Вейса (несовпадение версий с BOM) — ошибка «receiver type
  mismatch». Использовать `Icons.Filled.ArrowBack` и т.п. (deprecated-
  предупреждение — это нормально, не ошибка сборки).
- `json_encode()`-подобных проблем в Kotlin нет (это специфика PHP из
  веб-CRM), но по аналогии — если где-то видите вставку строки с
  кавычками прямо в `onclick`-подобную конструкцию, проверяйте
  экранирование.
- Android Studio один раз падал с «An internal error has occurred» —
  причина была именно в сломанном `androidx.browser` (см. выше),
  плюс не помешает `File → Invalidate Caches` после правки
  `build.gradle.kts`.

## 6. Дизайн

Material 3, Jetpack Compose, фирменные цвета (см. §3). Полный текст
промта для Gemini с описанием экранов/анимаций/иконок — в
`docs/MOBILE_API.md` репозитория `cms_avior`, раздел «Что попросить
у Gemini».

## 7. Упрощение формы заказа — 19.08

Решили, что приложение нужно только для оперативного занесения заказа
«с колёс», всё остальное (аналитика, статусы, документы) — на десктопе.
`AddOrderScreen` упрощён:
- Убран выбор «существующий/новый клиент» — теперь всегда просто поле
  ФИО + обязательный телефон. Дубли клиентов решаются **на сервере**,
  не в приложении: `find_or_create_client()` в `functions.php` (репо
  `cms_avior`) сравнивает по нормализованному телефону и переиспользует
  существующую запись, если найдёт совпадение — применено сразу во
  всех 4 точках создания заказа (веб-формы + Mobile API), не только
  в приложении.
- Тип устройства — теперь выпадающий список (`meta.device_types`),
  без ручного ввода — те же категории, что в веб-CRM.
- Модель — свободный ввод, но с живыми подсказками из каталога моделей
  (`meta.device_models`, ~200 записей, фильтруются на лету на клиенте).
- `MetaResponse` (`ApiModels.kt`) дополнен полями `deviceTypes`/
  `deviceModels` — сервер (`api/mobile/meta.php`) их уже отдаёт.

## 7.1. Подсказка «Найден: ...» по телефону — 19.08

При вводе телефона в `AddOrderScreen` — если он совпал с уже
существующим клиентом (сравнение по нормализованным цифрам, формат
записи не важен), показывается карточка «Найден: Иванов И.И.» с
предложением подставить имя одним тапом (не обязательно — можно
проигнорировать/скрыть, сервер и так не создаст дубль по телефону).
- **Сервер**: новый параметр `clients.php?phone=` (точный поиск, не
  путать с нечётким `?q=`) — репозиторий `cms_avior`, коммит `ebad5ca`.
- **Приложение**: `CmsViewModel.searchClientByPhone()` — debounce 500мс,
  срабатывает только от 10 цифр (не долбит сервер на каждый символ),
  отменяет предыдущий незавершённый запрос. Подсказка скрывается сама,
  если `clientName` уже заполнено (не мешает, если и так печатают своё),
  и очищается при уходе с экрана (`DisposableEffect`).

## 7.2. Баг: список клиентов «обрывался на середине алфавита» — 19.08

Причина: `clients.php` без `?q=` сортировал по `created_at DESC` с
`LIMIT 50` — при заходе в «Клиенты» видны были только 50 последних по
дате создания, а не по алфавиту, и выглядело это как «дальше буквы N
никого нет». Фикс в `cms_avior` (коммит `c9d6148`): единая сортировка
по `full_name` и с `?q=`, и без, лимит поднят до 1000 (клиентов сейчас
~150-200, запас на годы).

## 7.3. Анимации — 19.08

- **Переходы между экранами** — заданы один раз на `NavHost` в
  `MainActivity.kt` (`enterTransition`/`exitTransition`/
  `popEnterTransition`/`popExitTransition`), применяются автоматически
  ко всем экранам. Вперёд — новый выезжает справа; назад — зеркально.
- **Нажатие кнопок** — новый переиспользуемый модификатор
  `Modifier.pressScale()` в `Common.kt` (лёгкое сжатие при тапе, поверх
  стандартной ряби Material3). Применён пока только к кнопке входа и
  «Создать заказ» — на остальные кнопки добавляется одной припиской
  `.pressScale()` в конце цепочки модификаторов, при желании легко
  раскатить шире.

## 7.4. Мелкая правка от Вейса — 17.08 (коммит `a137ee9`)

Не от меня — Вейс сам поправил `OrderScreens.kt`: вынес три отдельных
`@OptIn(ExperimentalMaterial3Api::class)` (по одному на каждую функцию)
в один `@file:OptIn(...)` в начале файла, и добавил недостающую
аннотацию `@Composable` у `OrderDetailScreen` (без неё функция не
компилировалась бы как composable). Чисто рефакторинг/фикс, бизнес-
логику не менял. Упоминаю здесь, чтобы не удивляться при следующем
просмотре git log, откуда взялся коммит не от `Avior CMS Bot`.

## 7.5. Быстрая связь с клиентом — 19.08

В `OrderDetailScreen`, прямо под именем клиента в карточке заказа —
4 иконки: 📞 Позвонить (`Intent.ACTION_DIAL`), 💬 SMS (`smsto:`),
WhatsApp (`https://api.whatsapp.com/send?phone=`), Telegram
(`tg://resolve?phone=` — работает, только если у клиента номер
привязан к аккаунту Telegram и не скрыт настройками приватности; если
нет — тост с понятным объяснением, не тихий провал).

**Иконки WhatsApp/Telegram — настоящие логотипы**, не общие Material-
значки (`Chat`/`Send`) — Material Icons принципиально не включает
чужие товарные знаки. Взяты контуры у Bootstrap Icons (MIT-лицензия,
свободно для коммерческого использования): `res/drawable/ic_whatsapp.xml`,
`res/drawable/ic_telegram.xml` — `pathData` вставлен программно
(Python-скрипт, не руками), с посимвольной сверкой против оригинала —
при ручном переписывании длинных SVG-путей уже дважды словил
опечатку (задвоенная команда `c` в пути), поэтому теперь так не
делаю никогда. В коде — `Icon(painterResource(id = R.drawable.ic_whatsapp), ...)`.

## 8. Что дальше

1. Собрать в Android Studio, установить на телефон, проверить, что
   счётчики на панели больше не «живут своей жизнью» (§4).
2. Дальше — обычная разработка: правки через этот чат или напрямую,
   не забывать `git pull`/`git push` (см. памятку по командам, которую
   Вейс получил отдельно).
3. Обновлять этот файл после каждого значимого изменения — так же,
   как `cms_avior/PROJECT_STATE.md`.

## 9. Полезные ссылки

- API-документация и промт для Gemini: `cms_avior/docs/MOBILE_API.md`
- Веб-CRM (для сверки полей API, если что-то неясно): `cms_avior` репозиторий,
  особенно `public/api/mobile/*.php`
